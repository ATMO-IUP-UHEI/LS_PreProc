disconnect this from instrument type (in path i guess it still counts?)
as input give a L1B file and then put ATM at the same location

would eb sick if remotec changed name from

L1B_scenario.nc
ATM_scenario.nc

to

SPECTRA.nc
ATMOSPHERE.nc

disconnect the preproc l1b from the path as well, shouldn't go into the settings file instead input parameter
